on: 
  schedule:
  - cron: "40 2 * * *"
  workflow_dispatch:
jobs:
  dailysign:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.x'
      - run: python checkin.py
